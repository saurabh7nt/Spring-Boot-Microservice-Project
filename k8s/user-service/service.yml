apiVersion: v1
kind: Service
metadata:
  name: user-service-svc
spec:
  selector:
    app: user-service
  ports:
    - name: user-service-port
      protocol: TCP
      port: 8081
      targetPort: 8081
  type: ClusterIP
