apiVersion: v1
kind: Service
metadata:
  name: postgres-db-svc
spec:
  selector:
    app: postgres-db
  ports:
    - name: postgres-db-svc-port
      protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP
